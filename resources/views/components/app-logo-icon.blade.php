{{-- The Center's seal. It carries its own colours, so nothing here tints
     it — pass a size and leave it alone. --}}
<img src="{{ asset('images/logo.png') }}"
    alt="{{ __('Treatment and Rehabilitation Center Caraga') }}"
    {{ $attributes->class('object-contain') }} />
